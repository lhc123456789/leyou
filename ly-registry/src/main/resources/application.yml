server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false #注册自己
    fetch-registry: false #不拉取列表
  server:
    peer-node-read-timeout-ms: 1000 #http参数，读超时时间